2022 02 02

- 말이 이동할때 칸의 이동 값을 일반화 하기 어려웠는데, 윷놀이 관련 Baekjoon문제가 있어서 그 문제를 해설한 블로그를 찾아보다가 좋은 케이스를 얻어 구현하였다.

- enum을 int값으로 받아오는 방법을 알게되었다. 

- enum값중 랜덤으로 값을 뽑는법을 알게 되었다. getvalue()[인덱스] 를 활용해서 범위내의 랜덤 인덱스를 만들어내어 값을 추출한다.
 
- Tests: no tests found for given includes 에러 ->  테스트코드에서 junit을 사용한다는 설정을 gradle에 추가해주면 해결이 가능하다. 또는 설정에서 빌드설정에서 인텔리j로 바꿔도 해결가능.

2022 02 03

- Math.random() 메소드를 공부하였다, 0이상 1미만의 double값을 무작위로 리턴한다. 기존 랜덤윷을 제거하고, 윷하나당 앞면 뒷면을 뽑아 결과를 만들어냈다.
